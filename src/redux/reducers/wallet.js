const ESTADO_INICIAL = {
  currencies: [],
  expenses: [],
  editor: false,
  idToEdit: 0,
};

const wallet = (state = ESTADO_INICIAL, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default wallet;
